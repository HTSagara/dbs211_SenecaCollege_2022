SELECT count(*) FROM dbs211_ORDERS;
DESCRIBE dbs211_customers; -- gives the description of the table


SELECT * FROM DBS211_ORDERS;
SELECT * FROM DBS211_PAYMENTS;
SELECT * FROM USER_TABLES WHERE TABLE_NAME='DEPARTMENTS';
SELECT * FROM USER_TABLES WHERE TABLE_NAME='DBS211_EMPLOYEES';
SELECT * FROM USER_TABLES; --lists all the tables inside the schema
/*
INSERT INTO departments (deptno, name, location) VALUES
    (1, 'Finance', 'New York');
*/

